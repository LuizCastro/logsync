{
  "id": "synapse-oci-001",
  "name": "Synapse — OCI Decision Intelligence",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "synapse-meeting",
        "options": {},
        "responseMode": "responseNode"
      },
      "id": "1",
      "name": "Meeting Recording Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [240, 300],
      "webhookId": "synapse-meeting"
    },
    {
      "parameters": {
        "jsCode": "// Transcribe audio via Whisper if transcript not provided\nconst input = $input.first().json;\n\n// If transcript already provided, pass through\nif (input.transcript && input.transcript.length > 0) {\n  return [{ json: { ...input, source: input.source || 'meeting' } }];\n}\n\n// If audio_url provided, transcribe via Whisper\nif (input.audio_url) {\n  const resp = await fetch('http://synapse-whisper:9000/asr', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      audio_url: input.audio_url,\n      language: 'pt',\n      task: 'transcribe'\n    })\n  });\n  const result = await resp.json();\n  return [{ json: { ...input, transcript: result.text || '', source: 'meeting' } }];\n}\n\n// If audio_base64 provided, transcribe via Whisper\nif (input.audio_base64) {\n  const resp = await fetch('http://synapse-whisper:9000/asr', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      audio_data: input.audio_base64,\n      language: 'pt',\n      task: 'transcribe'\n    })\n  });\n  const result = await resp.json();\n  return [{ json: { ...input, transcript: result.text || '', source: 'meeting' } }];\n}\n\n// No transcript or audio provided\nreturn [{ json: { ...input, transcript: '', source: 'meeting' } }];"
      },
      "id": "9",
      "name": "Whisper Transcribe",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [480, 300]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "synapse-slack",
        "options": {},
        "responseMode": "responseNode"
      },
      "id": "2",
      "name": "Slack Event Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [240, 520],
      "webhookId": "synapse-slack"
    },
    {
      "parameters": {
        "jsCode": "// Skip Slack bot messages and short texts\nconst e = $input.first().json;\nif (e.bot_id || e.type === 'url_verification' || (e.text||'').length < 10) {\n  return [{ json: { skip: true } }];\n}\nreturn [{ json: {\n  source: 'slack',\n  channel: e.channel || 'unknown',\n  user: e.user || e.username || 'unknown',\n  text: e.text,\n  timestamp: e.ts || new Date().toISOString(),\n  thread_ts: e.thread_ts || null\n}}];"
      },
      "id": "3",
      "name": "Prepare Slack Event",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [480, 520]
    },
    {
      "parameters": {
        "jsCode": "// Build extraction prompt for OCI LLM\nconst input = $input.first().json;\nconst isSlack = input.source === 'slack';\n\nconst systemPrompt = `You are Synapse, a Decision Intelligence Engine.\nExtract STRUCTURED DECISIONS from the text below.\n\nFor each DECISION return JSON with:\n- title: Short imperative title\n- decision: What was decided (one sentence)\n- rationale: WHY (the reasoning)\n- alternatives_rejected: Options considered and rejected\n- owner: Responsible person (if mentioned)\n- action_items: Concrete next steps\n- confidence: 0.0-1.0\n- related_topics: Keywords\n\nRULES:\n- DECISION = commitment to action. 'We should consider X' ≠ decision.\n- Only real decisions with confidence >= 0.5\n- Return valid JSON: {\"decisions\": [{title, decision, rationale, alternatives_rejected, owner, action_items, confidence, related_topics}]}`;\n\nconst userPrompt = isSlack\n  ? `Slack message from #${input.channel} by ${input.user}:\\n\\n${input.text}`\n  : `Meeting transcript:\\n\\n${input.text || input.transcript || 'No transcript provided'}`;\n\nreturn [{ json: {\n  messages: [\n    { role: 'system', content: systemPrompt },\n    { role: 'user', content: userPrompt }\n  ],\n  source: input.source,\n  channel: input.channel || null,\n  meeting_id: input.meeting_id || null,\n  user: input.user || null\n}}];"
      },
      "id": "4",
      "name": "Build Extraction Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [720, 380]
    },
    {
      "parameters": {
        "model": "meta.llama-3.1-70b-instruct",
        "options": {
          "temperature": 0.1,
          "maxTokens": 2000
        }
      },
      "id": "5",
      "name": "OCI GenAI LLM",
      "type": "n8n-nodes-oci-generative-ai.chatModel",
      "typeVersion": 1,
      "position": [960, 380],
      "credentials": {
        "ociGenerativeAiApi": {
          "id": "REPLACE_WITH_CREDENTIAL_ID",
          "name": "OCI Generative AI"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Parse LLM response into structured decisions\nconst response = $input.first().json;\nconst content = response.text || response.choices?.[0]?.message?.content || '';\nconst meta = $('Build Extraction Prompt').first().json;\n\nlet parsed;\ntry {\n  // Try to extract JSON from response (may be wrapped in markdown)\n  const jsonMatch = content.match(/\\{[\\s\\S]*\\}/);\n  parsed = JSON.parse(jsonMatch ? jsonMatch[0] : content);\n} catch(e) {\n  return [{ json: { error: 'Parse failed', raw: content.substring(0, 500) } }];\n}\n\nconst decisions = (parsed.decisions || []).filter(d => d.confidence >= 0.5);\nif (decisions.length === 0) {\n  return [{ json: { status: 'no_decisions', source: meta.source } }];\n}\n\nconst ts = new Date().toISOString();\nreturn decisions.map(d => ({ json: {\n  id: `${meta.source}_${Date.now()}_${Math.random().toString(36).substr(2,6)}`,\n  title: d.title,\n  decision: d.decision,\n  rationale: d.rationale || '',\n  alternatives_rejected: Array.isArray(d.alternatives_rejected) ? d.alternatives_rejected : [],\n  owner: d.owner || meta.user || null,\n  action_items: Array.isArray(d.action_items) ? d.action_items : [],\n  confidence: d.confidence,\n  related_topics: Array.isArray(d.related_topics) ? d.related_topics : [],\n  source: meta.source,\n  channel: meta.channel,\n  meeting_id: meta.meeting_id,\n  created_at: ts\n}}));"
      },
      "id": "6",
      "name": "Parse Decisions",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [1200, 380]
    },
    {
      "parameters": {
        "jsCode": "// Store decisions to SQLite via HTTP API\nconst decisions = $input.all().filter(i => !i.json.error && i.json.status !== 'no_decisions');\nconst results = [];\n\nfor (const item of decisions) {\n  const d = item.json;\n  \n  // Store decision via API\n  try {\n    const resp = await fetch('http://synapse-api:8080/api/decisions', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify(d)\n    });\n    const saved = await resp.json();\n    \n    // Store action items\n    if (d.action_items?.length > 0) {\n      for (const action of d.action_items) {\n        const actionText = typeof action === 'string' ? action : action.action || action;\n        await fetch('http://synapse-api:8080/api/actions', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({\n            decision_id: saved.id,\n            decision_title: d.title,\n            action: actionText,\n            owner: typeof action === 'string' ? d.owner : (action.owner || d.owner),\n            priority: d.confidence >= 0.8 ? 'high' : 'medium'\n          })\n        });\n      }\n    }\n    \n    results.push({ json: { stored: true, id: saved.id, title: d.title } });\n  } catch(e) {\n    results.push({ json: { stored: false, error: e.message, title: d.title } });\n  }\n}\n\nreturn results.length > 0 ? results : [{ json: { status: 'nothing_to_store' } }];"
      },
      "id": "7",
      "name": "Store to SQLite",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [1440, 380]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ status: 'ok', decisions: $input.all().map(i => i.json) }) }}",
        "options": {}
      },
      "id": "8",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1680, 380]
    }
  ],
  "connections": {
    "Meeting Recording Webhook": {
      "main": [[{ "node": "Whisper Transcribe", "type": "main", "index": 0 }]]
    },
    "Slack Event Webhook": {
      "main": [[{ "node": "Prepare Slack Event", "type": "main", "index": 0 }]]
    },
    "Prepare Slack Event": {
      "main": [[{ "node": "Build Extraction Prompt", "type": "main", "index": 0 }]]
    },
    "Whisper Transcribe": {
      "main": [[{ "node": "Build Extraction Prompt", "type": "main", "index": 0 }]]
    },
    "Build Extraction Prompt": {
      "main": [[{ "node": "OCI GenAI LLM", "type": "main", "index": 0 }]]
    },
    "OCI GenAI LLM": {
      "main": [[{ "node": "Parse Decisions", "type": "main", "index": 0 }]]
    },
    "Parse Decisions": {
      "main": [[{ "node": "Store to SQLite", "type": "main", "index": 0 }]]
    },
    "Store to SQLite": {
      "main": [[{ "node": "Respond to Webhook", "type": "main", "index": 0 }]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [{ "name": "synapse" }, { "name": "oci" }]
}
